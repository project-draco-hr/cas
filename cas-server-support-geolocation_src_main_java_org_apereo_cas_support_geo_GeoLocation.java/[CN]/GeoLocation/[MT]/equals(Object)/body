{
  if (obj == null) {
    return false;
  }
  if (obj == this) {
    return true;
  }
  if (obj.getClass() != getClass()) {
    return false;
  }
  final GeoLocation rhs=(GeoLocation)obj;
  return new EqualsBuilder().append(this.city,rhs.city).append(this.country,rhs.country).isEquals();
}
