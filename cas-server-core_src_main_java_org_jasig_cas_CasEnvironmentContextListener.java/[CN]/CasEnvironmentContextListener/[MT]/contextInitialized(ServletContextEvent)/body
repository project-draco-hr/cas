{
  final ApplicationContext ctx=WebApplicationContextUtils.getWebApplicationContext(event.getServletContext());
  LOGGER.info("[{}] has loaded the CAS servlet application context: {}",event.getServletContext().getServerInfo(),ctx);
}
