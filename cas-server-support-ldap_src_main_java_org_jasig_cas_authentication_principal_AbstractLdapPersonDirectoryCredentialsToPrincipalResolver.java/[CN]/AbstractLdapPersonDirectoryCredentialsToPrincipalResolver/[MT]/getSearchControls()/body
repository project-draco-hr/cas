{
  final SearchControls constraints=new SearchControls();
  logger.debug("returning search controls: scope={}; search base={}; attributes={}; timeout=",this.scope,this.searchBase,Arrays.toString(this.attributeIds),this.timeout);
  constraints.setSearchScope(this.scope);
  constraints.setReturningAttributes(this.attributeIds);
  constraints.setTimeLimit(this.timeout);
  constraints.setCountLimit(DEFAULT_MAX_NUMBER_OF_RESULTS);
  return constraints;
}
