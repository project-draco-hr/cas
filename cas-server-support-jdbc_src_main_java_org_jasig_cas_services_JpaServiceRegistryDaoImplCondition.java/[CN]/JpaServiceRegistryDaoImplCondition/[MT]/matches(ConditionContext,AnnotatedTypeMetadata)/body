{
  final BeanFactory factory=conditionContext.getBeanFactory();
  final BeanDefinitionRegistry registry=conditionContext.getRegistry();
  return factory.containsBean("serviceEntityManagerFactory") || registry.containsBeanDefinition("serviceEntityManagerFactory");
}
