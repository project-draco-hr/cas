{
  final PrincipalAttributesRepository rep=new DefaultPrincipalAttributesRepository(Collections.<String,Object>singletonMap("mail","final@example.com"));
  assertEquals(rep.getAttributes("uid").size(),1);
  assertTrue(rep.getAttributes("uid").containsKey("mail"));
}
