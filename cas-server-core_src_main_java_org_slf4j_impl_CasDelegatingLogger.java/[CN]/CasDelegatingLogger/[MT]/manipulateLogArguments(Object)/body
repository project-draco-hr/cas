{
  for (int i=0; i < args.length; i++) {
    if (args[i] != null) {
      final String objStr=removeTicketId(args[i].toString());
      args[i]=objStr;
    }
 else {
      args[i]=args[i];
    }
  }
  return args;
}
