{
  try {
    out.warn(ANSI_CYAN);
    out.warn("\n\n" + FigletFont.convertOneLine(asciiArt) + additional);
    out.warn(ANSI_RESET);
  }
 catch (  final Exception e) {
    throw Throwables.propagate(e);
  }
}
