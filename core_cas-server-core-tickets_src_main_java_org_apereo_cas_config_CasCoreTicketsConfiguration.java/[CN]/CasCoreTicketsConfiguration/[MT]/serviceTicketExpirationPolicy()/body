{
  return new MultiTimeUseOrTimeoutExpirationPolicy.ServiceTicketExpirationPolicy(casProperties.getTicket().getSt().getNumberOfUses(),TimeUnit.SECONDS.toMillis(casProperties.getTicket().getSt().getTimeToKillInSeconds()));
}
