{
  final ModelAndView mv=this.mvc.perform(get("/manage.html")).andExpect(status().isOk()).andReturn().getModelAndView();
  assertNotNull(mv);
  assertNotNull(mv.getModel().get("defaultServiceUrl"));
  final List<?> svcs=(List<?>)mv.getModel().get("services");
  assertEquals(svcs.size(),1);
}
