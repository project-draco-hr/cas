{
  final String requestId=kryo.readObjectData(buffer,String.class);
  final String relayState=kryo.readObjectData(buffer,String.class);
  try {
    final GoogleAccountsService service=(GoogleAccountsService)CONSTRUCTOR.newInstance(id,originalUrl,artifactId,relayState,requestId,privateKey,publicKey,alternateUsername);
    return service;
  }
 catch (  Exception e) {
    throw new IllegalStateException("Error creating SamlService",e);
  }
}
