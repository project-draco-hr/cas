{
  if (obj == null) {
    return false;
  }
  if (obj == this) {
    return true;
  }
  if (obj.getClass() != getClass()) {
    return false;
  }
  final RadiusMultifactorAuthenticationProvider rhs=(RadiusMultifactorAuthenticationProvider)obj;
  return new EqualsBuilder().append(this.rank,rhs.rank).append(this.getId(),rhs.getId()).isEquals();
}
