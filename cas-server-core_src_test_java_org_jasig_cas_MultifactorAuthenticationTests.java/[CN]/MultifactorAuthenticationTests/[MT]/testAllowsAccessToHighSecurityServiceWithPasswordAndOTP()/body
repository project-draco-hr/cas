{
  final TicketGrantingTicket tgt=cas.createTicketGrantingTicket(newUserPassCredentials("alice","alice"),new OneTimePasswordCredential("alice","31415"));
  assertNotNull(tgt);
  final String st=cas.grantServiceTicket(tgt.getId(),newService("https://example.com/high/"));
  assertNotNull(st);
}
