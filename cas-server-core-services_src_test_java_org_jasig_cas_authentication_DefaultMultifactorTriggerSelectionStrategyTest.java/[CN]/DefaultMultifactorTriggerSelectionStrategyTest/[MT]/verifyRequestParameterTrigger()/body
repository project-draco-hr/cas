{
  assertThat(strategy.resolve(VALID_PROVIDERS,mockRequest(MFA_VALID_1),null,null).orElse(null),is(MFA_VALID_1));
  assertThat(strategy.resolve(VALID_PROVIDERS,mockRequest(MFA_VALID_2),null,null).orElse(null),is(MFA_VALID_2));
  assertThat(strategy.resolve(VALID_PROVIDERS,mockRequest(MFA_INVALID),null,null).orElse(null),nullValue());
}
