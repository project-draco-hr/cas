{
  final Principal principal=new SimplePrincipal("bob",Collections.singletonMap("displayName",(Object)"Bob"));
  return new TicketGrantingTicketImpl(idGenerator.getNewTicketId("TGT"),TestUtils.getAuthentication(principal),expirationPolicyTGT);
}
