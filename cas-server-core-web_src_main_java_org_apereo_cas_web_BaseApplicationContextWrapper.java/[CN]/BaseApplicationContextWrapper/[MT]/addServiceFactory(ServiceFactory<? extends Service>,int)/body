{
  logger.debug("Adding [{}] application context",factory);
  final List<ServiceFactory<? extends Service>> list=this.applicationContext.getBean("serviceFactoryList",List.class);
  list.add(index,factory);
}
