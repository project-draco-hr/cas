{
  try {
    return new HttpBasedServiceCredential(new URL(url),org.jasig.cas.services.TestUtils.getRegisteredService(url));
  }
 catch (  final MalformedURLException e) {
    throw new IllegalArgumentException();
  }
}
