{
  final AdaptiveMultifactorAuthenticationWebflowEventResolver r=new AdaptiveMultifactorAuthenticationWebflowEventResolver();
  configureResolver(r);
  r.setGeoLocationService(this.geoLocationService);
  return r;
}
