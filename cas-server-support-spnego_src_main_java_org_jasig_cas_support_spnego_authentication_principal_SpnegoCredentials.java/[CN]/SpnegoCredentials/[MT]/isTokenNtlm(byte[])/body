{
  if (token == null || token.length < 8) {
    return false;
  }
  for (int i=0; i < 8; i++) {
    if (SpnegoConstants.NTLMSSP_SIGNATURE[i] != token[i]) {
      return false;
    }
  }
  return true;
}
