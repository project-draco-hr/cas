{
  if (this.refreshIntervalInMinutes > 0 && this.metadataRefreshedDateTime.plusMinutes(this.refreshIntervalInMinutes).isBeforeNow()) {
    logger.info("Metadata was last refreshed at [{}]. Refreshing...",this.metadataRefreshedDateTime);
    return true;
  }
  return false;
}
