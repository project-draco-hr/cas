{
  this.ticket.grantServiceTicket("test",TestUtils.getService(),this.expirationPolicy,false,TestUtils.getDefaultRegisteredService());
  Thread.sleep(TIMEOUT - TIMEOUT_BUFFER);
  assertFalse(this.ticket.isExpired());
}
