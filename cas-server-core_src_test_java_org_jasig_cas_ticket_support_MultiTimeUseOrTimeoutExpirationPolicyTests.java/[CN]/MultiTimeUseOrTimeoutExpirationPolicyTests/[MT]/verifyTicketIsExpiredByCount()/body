{
  for (int i=0; i < NUMBER_OF_USES; i++) {
    this.ticket.grantServiceTicket("test",TestUtils.getService(),new NeverExpiresExpirationPolicy(),false,TestUtils.getDefaultRegisteredService());
  }
  assertTrue(this.ticket.isExpired());
}
