{
  final List<OAuthCodeImpl> oAuthCodeImpls=getTicketQueryResultList(ticketId,"select o from OAuthCodeImpl o where o.id = :id",OAuthCodeImpl.class);
  return deleteTicketsFromResultList(oAuthCodeImpls);
}
