{
  final List<ServiceTicketImpl> serviceTicketImpls=getTicketQueryResultList(ticketId,"select s from ServiceTicketImpl s where s.id = :id",ServiceTicketImpl.class);
  return deleteTicketsFromResultList(serviceTicketImpls);
}
