{
  return new MongoServiceRegistryDao(mongoTemplate(),casProperties.getServiceRegistry().getMongo().getServiceRegistryCollection(),casProperties.getServiceRegistry().getMongo().isDropCollection());
}
