{
  LOGGER.warn("[{}] is designed to retrieve and persist service definitions in memory. Changes that are made to service definitions during runtime " + "will be LOST upon container restarts.",InMemoryServiceRegistryDaoImpl.class.getName());
}
