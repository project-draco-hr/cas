{
  final PrincipalBearingCredential credentials=new PrincipalBearingCredential(DefaultPrincipalFactory.getInstance().createPrincipal("scott"));
  assertNotNull(this.handler.authenticate(credentials));
}
