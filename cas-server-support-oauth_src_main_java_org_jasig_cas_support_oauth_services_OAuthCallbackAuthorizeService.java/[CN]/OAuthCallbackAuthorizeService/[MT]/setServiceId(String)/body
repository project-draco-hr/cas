{
  if (!id.endsWith(OAuthConstants.CALLBACK_AUTHORIZE_URL)) {
    final String msg=String.format("OAuth callback authorize service id must end with [%s]",OAuthConstants.CALLBACK_AUTHORIZE_URL);
    throw new IllegalArgumentException(msg);
  }
  super.setServiceId(id);
}
