{
  return new OAuthAccessTokenExpirationPolicy(casProperties.getAuthn().getOauth().getAccessToken().getMaxTimeToLiveInSeconds(),casProperties.getAuthn().getOauth().getAccessToken().getTimeToKillInSeconds(),TimeUnit.SECONDS);
}
