{
  if (this.authentications.add(authentication)) {
    LOGGER.debug("Collected authentication event. Associated principal with this authentication is [{}]",authentication.getPrincipal());
  }
 else {
    LOGGER.warn("Failed to collect duplicate authentication event as it already exists.");
  }
  return this;
}
